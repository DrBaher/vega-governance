# Universal Rules — All Agents

Applies to every agent. Each traced to a real failure. See architecture §3 for full text.

## U1-U10 Summary
U1: Verify before applying | U2: Read end-to-end not diffs | U3: Trace = open files | U4: No domain standard IDs from memory | U5: Break comfortable patterns | U6: Sequential vs competing | U7: Compression produces confabulation | U8: Existing mechanisms first | U9: Three Phase 2s | U10: Propagation through ALL documents

## Spec Interpretation — Cross-Agent

### Three Phase 2s (CRITICAL — RC-05, RC-06)
1. System Phase 2: product LLM modes A/B/C. Running on deployment platform.
2. Build Phase 2 (build quality checks): DB construction quality checks. 5 sub-steps.
3. Phase 2 of corrections: next validation cycle after Phase 1 passes.

### Document A vs Document B
data specification = Document A (SOPs). Configuration v1.0 = Document B (parameters, 97-class list).

### PARTIAL entries score 1.0 on axes they have.

### OPERATOR_CONFIRMED rolled back → flag override mechanism.

### Active documents (authority)
scope specification.2, data specification v1.1, technical specification v4.5, Client API v3.5, Phase 2 Config v1.1, Internal_DB_Construction_Tooling v1.0, Build Strategy, open issues register, session handoff, Product Walkthrough, Project Audit Archive, Project Overview, Spec Change History, Domain Expert Answers Gap Analysis.

### LCN text ≠ Part assignment
Domain reference data is the ONLY authority for identifier assignments — not display names or common usage.

### Unit-to-Property: no ambiguity for dimensional units
[unit] = [property] always. mmol/L = [property] always. Genuinely ambiguous: %, ratio, titer.

### Biologie FRA and JDV are domain standard-official ([decision])
Layer 1 (process spec.2), NOT Layer 2+ terminology-mapped.
